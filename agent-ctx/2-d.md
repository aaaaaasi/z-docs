# Task 2-d — Recent Activity feed, Settings experience, App launcher grid

**Task ID:** 2-d
**Agent:** 2-d (activity + settings + app-grid)
**Task:** Build the Recent activity feed (`ActivityView`), the Settings experience (`SettingsView` + `SettingsEffects` + `settings-lib`), and the app-launcher grid (`AppGridMenu`) for the Z workspace (Z-Docs/Z-Sheets/Z-Slides/Z-Forms).

## Work Log

- Read worklog.md (incl. WA-0 contract), docs-store.ts, workspace-types.ts, api-client.ts, identity.ts, doc-utils.ts, user-menu.tsx, theme-toggle.tsx, home-header.tsx, providers.tsx, globals.css (READ ONLY), ui/ listing; verified `/api/activity` + `/api/export` route implementations and shadcn exports (alert-dialog, select, alert, skeleton, label, separator, tooltip), `useToast` hook, date-fns v4 (`isToday`/`isYesterday`/`differenceInCalendarDays`).
- **NEW `src/components/docs/activity/activity-meta.ts`** — shared `APP_META` (docs=teal #0b6b62 / sheets=green #1f7a3f / slides=amber #b26a00 / forms=rose #a8435a, icons FileText/FileSpreadsheet/Presentation/FormInput, labels + chip labels) and `KIND_META` (verb + icon per ActivityKind: Plus/Pencil/Tag/Star(filled)/Star/Trash2/History/Copy/MessageSquare/ClipboardCheck/Share2). Also imported by app-grid.tsx (no file created outside allowed paths).
- **NEW `src/components/docs/activity/activity-view.tsx`** — `export function ActivityView()` (no props): h-16 sticky header (ArrowLeft back → goHome with "Back" tooltip, "Recent activity" + subtitle, RefreshCw that spins while loading); filter chips row (All + Docs/Sheets/Slides/Forms pills, icon + tnum count badge, active = bg-primary text-primary-foreground; counts computed client-side from a parallel full-feed fetch); timeline max-w-3xl grouped by day (Today / Yesterday / weekday within 7d / "Mon 3 Jun" beyond) with sticky day sub-headers (top-16); rows = 24px actor avatar (initialsOf + actor color) with a 16px app-colored corner badge carrying the kind icon, 13px sentence “**You** verb **“Title”**” (actor bold, verb muted, title quoted medium; “You” detected via local user id), detail as muted second line, right-aligned tnum relative time with fullTime tooltip; rows with entityId are buttons (hover:bg-accent/60) → docs: `openDoc(entityId)`, others: `openApp(app, { id })`, trashed/duplicated still open; no-entityId rows static. States: 8 skeleton rows (animate-pulse), empty state (FileClock in primary/10 circle + “No activity yet” + hint + “Back to home” ghost button), destructive Alert with Try again. Fetch on mount + filter change + refresh (no polling); out-of-order response guard via request id ref; stale-while-revalidate keeps the list visible when re-fetching the same filter.
- **NEW `src/components/docs/settings/settings-lib.ts`** — localStorage keys (`zdocs-user`, `zdocs-accent`, `zdocs-default-font`, `zdocs-default-zoom`, `zdocs-theme-mode`), `ACCENT_OPTIONS` (Teal #0b6b62 / Forest #3f6b35 / Amber #a8601a / Rose #a8435a / Plum #7a4a8a — no blues), `readStoredAccent`, `applyAccent` (sets `--primary`/`--ring`/`--primary-foreground` inline on documentElement; theme-aware: light = raw hex + white fg, dark = hex lightened 45% toward white + dark tinted-ink fg so buttons stay readable), `saveAccent`, `clearAccent`.
- **NEW `src/components/docs/settings/settings-view.tsx`** — `export function SettingsView()` (no props): h-16 header (back → goHome, “Settings”, “Z-Docs v1.0” version chip); `max-w-5xl` layout with left tab nav (w-48, sticky top-24 on md+, horizontal scrollable row on mobile; tabs General/Appearance/Workspace defaults/Data & storage/About with User/Palette/FileCog/Database/Info icons, active = bg-accent rounded-md font-medium, role=tablist/tab + aria-selected); content cards `rounded-xl border bg-background p-6 space-y-6`. All controls are REAL:
  - **General/Profile:** 40px live avatar preview (initials + color update as you type), display-name Input saved via `saveLocalUser` with 400ms debounce + toast “Name saved”, PRESENCE_COLORS.slice(0,10) swatch grid (save + toast “Avatar color updated”, Check on selected), explanatory note.
  - **Appearance:** theme radio CARDS (Light/Dark/System) each with a mini mock-window preview (static hexes; System = half light/half dark), selected = ring-2 ring-primary + Check. Since the app ThemeProvider has `enableSystem={false}`, “System” is genuinely implemented via a `useThemeMode` hook (matchMedia resolution + live follow of OS preference changes + re-sync when the theme is toggled from the header), persisted to `zdocs-theme-mode`. Accent picker: 5 swatches → `saveAccent` (persist + immediate CSS var override) with Check on selected; re-applies on resolvedTheme change so light/dark each get the right variant.
  - **Workspace defaults:** Select Sans/Serif → `zdocs-default-font`; Select 100/125/150% → `zdocs-default-zoom`; “Used when creating new documents” hints; toasts on change (main agent wires consumption).
  - **Data & storage:** plain `fetch("/api/export")` (no download) → counts grid (documents/sheets/decks/forms/folders/tags mini stats, icons + tnum) + total JSON bytes (TextEncoder) → Google-Drive-style usage bar (h-2, bg-primary fill, width = min(100, bytes/10MB), progressbar role + aria) with “X.X KB used · 10 MB quota” tnum label; skeletons while loading, inline error otherwise. “Export all your data” triggers a real browser download via temporary `<a href="/api/export" download>` + toast “Download started”. “Reset local profile” → AlertDialog confirm → removes the 4 spec keys (+ `zdocs-theme-mode`) from localStorage → `location.reload()`.
  - **About:** product card (bg-primary square with blinking caret + font-editorial wordmark — recolors live with the accent), tagline “A Google-style workspace: Docs, Sheets, Slides & Forms”, tech chips (Next.js 16 · React 19 · Tailwind 4 · Prisma · TipTap-style canvas · socket.io collab), small print. No placeholder/“coming soon” text anywhere.
- **NEW `src/components/docs/settings/settings-effects.tsx`** — `export function SettingsEffects()` (renders null): on mount applies the stored accent and re-applies it whenever next-themes toggles the `dark` class on `<html>` (MutationObserver on the class attribute). To be mounted once at app root by the main agent.
- **NEW `src/components/docs/app-grid.tsx`** — `export function AppGridMenu()` (no props): ghost icon Button trigger (LayoutGrid, h-9 w-9 rounded-full, aria-label “Z workspace apps”); controlled Popover (w-72, p-3, rounded-xl, shadow-xl) with “Z Workspace” title, grid-cols-2 tiles (Z-Docs/Z-Sheets/Z-Slides/Z-Forms; 48px round icon in app-tinted soft bg via `color-mix(in srgb, color 14%, transparent)` + text-xs label, hover:bg-accent); click → `goHome()` for docs tile, `openApp(app)` for others, popover closes; Separator + two full-width rows: Recent activity (FileClock) → `openActivity()`, Settings (Settings) → `openSettings()`. Radix keyboard support; no placeholder text.
- **Verification:** `bunx eslint` on all my files/paths → zero errors; `bunx tsc --noEmit -p tsconfig.json` → zero src errors; extra SSR smoke render of all 4 components via renderToString in a scratch bun script (outside the repo) → all PASS (SettingsEffects intentionally renders null); content probes (titles/chips/labels) PASS. Scratch script deleted.

## Integration contract for the main agent

- `docs-app.tsx`: render `view === "activity" ? <ActivityView/>` and `view === "settings" ? <SettingsView/>` (inside the existing TooltipProvider), and mount `<SettingsEffects/>` once (e.g. next to DocsApp root).
- `home-header.tsx`: replace the dead LayoutGrid button with `<AppGridMenu/>` (and optionally point the Settings ghost button at `openSettings()`); import path `@/components/docs/app-grid`.
- **IMPORTANT — dev server is STALE:** the running dev server (started 10:26) predates the WA-0 Prisma client regen (10:49), so ALL new-model routes currently 500 (`db.activityLog is undefined` — Turbopack doesn't invalidate node_modules). Disk client is current (`activityLog in prismaClient === true` via node probe). A dev-server restart (documented orphaning-subshell pattern) is REQUIRED before any QA of activity/export/sheets/slides/forms.
- New localStorage keys: `zdocs-accent`, `zdocs-default-font`, `zdocs-default-zoom`, `zdocs-theme-mode` (consumed by `zdocs-default-font`/`zdocs-default-zoom` when creating new documents; reset in Settings clears all + `zdocs-user`).

## Stage Summary

- Delivered all four 2-d deliverables: full Recent-activity page (filter chips + counts, day-grouped sticky timeline, actor-avatar rows with app/kind badges, skeleton/empty/error states, click-through to editors), full Settings experience (profile with debounced save, live theme + accent system that genuinely works in light AND dark, workspace defaults, real export/download + reset flows, About card), the mount-time accent effects component, and the Google-style app launcher grid — all self-contained in `src/components/docs/activity/**`, `src/components/docs/settings/**`, `src/components/docs/app-grid.tsx` with zero edits to existing files.
- Accent system: CSS-var overrides (--primary/--ring/--primary-foreground) applied inline on documentElement, persisted to localStorage, theme-aware (dark gets a lightened accent + dark ink fg), applied at mount via SettingsEffects and re-applied on theme flips (MutationObserver).
- “System” theme works despite `enableSystem={false}` in the provider — resolved locally via matchMedia with live OS-preference following and external-toggle re-sync.
- Known gaps / notes: (1) dev server must be restarted to activate the WA-0 Prisma client (see above); (2) chip counts come from a second full-feed request (`?limit=200`) fetched in parallel with the filtered one — tiny overhead, keeps counts app-accurate while filtered; (3) activity `detail` line is one-line truncated for polish; (4) reset also clears `zdocs-theme-mode` (my addition) in addition to the 4 spec keys; (5) eslint + tsc + SSR smoke verified, full interactive QA pending main-agent wiring + server restart.
