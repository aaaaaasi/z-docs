---
Task ID: 16-d
Agent: forms+activity+settings i18n subagent
Task: Z-Forms + 动态 + 设置 中文翻译与语言设置区块

Work Log:
- Read context first: worklog.md (Tasks 2-c/2-d + WA-1), src/lib/i18n.tsx, dict-common.ts, lang-toggle.tsx, doc-utils.ts (relativeTime/fullTime accept lang), and all 16 files in my scope.
- Filled the two dictionaries I own (keys = exact English source strings, zh values in Google product style):
  - dict-forms.ts — 157 entries (list view, cards, top bar, builder, question editor incl. 6 question-type labels, fill view/renderer, responses analytics, app-shell + list-hook toasts, API error keys).
  - dict-apps.ts — 92 entries (activity header/states/chips/day-groups/verbs; settings tabs, profile, appearance, language section, workspace defaults, data & storage, about).
  - No re-adding of dict-common words (Save/Cancel/Star/Trash/Search/Settings/Language/中文/English/Light/Dark/System/Theme/You/Back…); no duplicate keys across my two dicts (verified by script).
- Module-scope data kept English per contract, translated at render via t(): QUESTION_TYPES + typeLabel (forms-utils), KIND_META verbs + FILTERS labels (activity), TABS/THEME_CARDS/ACCENT_OPTIONS(via settings-lib)/STORAGE_STATS (settings). No useI18n/t() at module scope anywhere.
- forms-utils.ts: stripped HTTP status codes from thrown user-facing Error messages so they become exact dict keys ("Failed to load form", "Form not found", "This form is closed and no longer accepts responses." etc.) and are translated at display time (t(e.message) / t(list.error)); CSV "Timestamp" header + "-responses.csv" filenames intentionally left English (export format).
- Components now call useI18n(): all 11 forms files, activity-view, all settings-view sections. Plurals handled with paired keys ("1 response"/"{n} responses", "1 response · edited {time}"/"{n} responses · edited {time}", "1 star"/"{n} stars", "1 response exported to {file}"/"{n} responses exported to {file}"). Toasts inside the FormsApp component use t from context; useFormsList (hook, non-component code) uses the mandated `tForLang(getCurrentLang(), …)` via a stable `tr` helper — duplicated form titles are created localized ("Copy of {title}" → "{title} 的副本"; new forms created as "未命名表单" in zh).
- Renamed local shadowing variables (`const t = setTimeout` → timer; `consumeAppTarget()` → target; TABS.map((t)→tabItem; QUESTION_TYPES.map((t)→qt; onTabChange={(t)→tab)) so the i18n `t` is never shadowed.
- Dates: every relativeTime/fullTime call in my scope now passes `lang` (form cards, responses list/summary, activity rows + tooltips).
- Activity day groups: dayKeyAndLabel/groupByDay now take (t, lang); Today/Yesterday via dict; <7 days → Intl weekday long; older → Intl weekday short + day + month short, using localeOf(lang). Group memo deps [items, t, lang]. Actor names/entity titles/detail (DB content) untouched; "You"/"Someone" via dict. Filter chips render t(label) (全部/文档/表格/幻灯片/表单). Fetch failure now throws a translatable generic key (displayed via t).
- Settings: all 5 tabs, profile fields/toasts, theme radio cards, accent names (青绿/森林绿/琥珀/玫瑰红/深紫), workspace defaults (fonts keep Arial/Georgia), storage stats/labels, export + reset AlertDialog fully translated; DataSection error now a plain dict key rendered via t().
- NEW Language section (real feature): Appearance tab renders a second SectionCard 「语言 / Language」 (description "选择工作区的显示语言。") with two radio cards styled identically to the theme cards (role=radiogroup/radio, aria-checked, ring+Check selected state, LanguagePreview box with a native sample line 你好，Z-Docs / Hello, Z-Docs). Clicking a card calls setLang from useI18n — switches the whole app instantly and persists to localStorage; native labels 中文/English hardcoded like lang-toggle.
- settings-lib.ts / settings-effects.tsx / activity-meta.ts intentionally untouched (accent/theme-only + English data records).
- Verification:
  1. `bunx tsc --noEmit` → 0 src errors (transient errors from the concurrent editor group's in-flight files resolved on re-run; my scope was clean at every check).
  2. `bun run lint` → clean; `bunx eslint` scoped to my 16 files → clean.
  3. Key-resolution script: every t()/tr() key in my scope resolves in the merged dictionary; cross-dict duplicate check → none.
  4. Throwaway SSR smoke (bun + renderToString, single React copy, deleted afterwards): FormsApp/FormsList/FormTopBar/FormCard/QuestionEditor/QuestionCard/FillView/ResponsesView/ActivityView/SettingsView/SettingsEffects all PASS with zh content probes (新建表单/已加星标/回收站/必填/添加选项/此题为必填/你的回答/切勿通过 Z-Forms 提交密码。/暂无回复/下载 CSV/平均作答率/近期动态/全部/文档/表格/幻灯片/表单/设置/常规/外观/…/语言/你好，Z-Docs/Hello, Z-Docs/2 份回复 · 编辑于). Radix Select closed-trigger label never SSRs (pre-existing behavior, not i18n-related).
- Note for main agent: the Next dev server on :3000 stopped listening around 14:12 (log ends with successful compiles, no error; collab :3003 still up). I did not restart it per task constraints — needs main-agent attention before browser QA.

Stage Summary:
- Z-Forms (list/builder/fill/responses), Recent activity and Settings are fully Chinese by default with instant English fallback — every user-visible string incl. placeholders, aria-labels, tooltips, toasts, empty/error/skeleton states, dialogs, tabs, sr-only and required-validation goes through the i18n system; DB content, product names (Z-Forms etc.), CSV export fields and font names stay untranslated by design.
- dict-forms.ts: 157 entries; dict-apps.ts: 92 entries; both verified collision-free vs dict-common and each other.
- New working Language settings block in Settings → Appearance (radio cards wired to setLang, theme-card styling).
- Quality gates green: tsc 0 src errors, lint clean, SSR smoke pass, all keys resolve.
