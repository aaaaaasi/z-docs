---
Task ID: 17-e1
Agent: security subagent (docs core API hardening)
Task: 用户最严重投诉「没有权限分化、没有防越权（全部公开可查看）、没有安全/防攻击机制」→ 给核心文档相关 API 路由加真实权限守卫（session 认证 + owner/editor/viewer 分级 + 同源 + 限流 + 活动归因防伪）
---

Work Log:

**改动文件（17 个，全部在授权清单内；`src/app/api/folders/[id]/route.ts` 不存在，无需处理）**
`src/lib/server-activity.ts`、`src/app/api/documents/route.ts`、`documents/[id]/route.ts`、`[id]/duplicate`、`[id]/versions`、`[id]/restore-version`、`[id]/collaborators`、`[id]/comments`、`[id]/tags`、`src/app/api/comments/[id]/route.ts`、`comments/[id]/reactions`、`src/app/api/folders/route.ts`、`src/app/api/tags/route.ts`、`tags/[id]/route.ts`、`src/app/api/activity/route.ts`、`src/app/api/export/route.ts`、`src/components/docs/editor/share-dialog.tsx`（仅 t() 包裹 API 英文错误）。

**逐路由守卫摘要（全部经 guardRoute：读 240/min，写 60/min + {mutating:true} 同源检查；401 "Sign in required." / 403 "You do not have permission to do that." / 404 notFound() 与 server-auth.ts 文案一致）**

| 路由 | 守卫 |
|---|---|
| GET /api/documents | 登录 + 列表可见性：非 admin 强制 `AND { OR:[{ownerId:user.id},{ownerId:null},{collaborators:{some:{email:user.email}}}] }`（与 filter/starred/trash/folder/q 以 AND 数组合并，避免与搜索 OR 顶层撞 key）；admin 全量可见 |
| POST /api/documents | mutating + `ownerId: user.id`（新建必归属创建者） |
| GET /api/documents/[id] | 登录 + docAccessLevel → none 返回 **404**（不暴露存在性） |
| PATCH /api/documents/[id] | mutating + none→404、非 editor→403；**原样保留** sanitizeDocHtml + editCount 递增 + maybeSnapshot + activity 分支 |
| DELETE /api/documents/[id] | mutating + none→404、非 editor→403 |
| POST /[id]/duplicate | mutating + editor+；副本 `ownerId: 复制者`（否则副本成为全员可见的无主文档——堵住一个越权放大洞） |
| GET /[id]/versions | viewer+ |
| POST /[id]/restore-version | mutating + editor+ |
| GET /[id]/collaborators | viewer+ |
| POST/DELETE /[id]/collaborators | mutating + **owner/admin**（邀请/移除协作者是所有者权限；editor 403 已实测） |
| GET /[id]/comments | viewer+ |
| POST /[id]/comments | mutating + editor+；作者身份一律取**会话用户**（覆盖 body authorId/authorName/authorColor，防身份伪造；原 "Author identity is required" 400 移除） |
| PATCH/DELETE /api/comments/[id] | mutating + 父文档 editor+ **且**（作者本人 或 owner/admin）；legacy 匿名评论归 owner/admin 管 |
| POST /api/comments/[id]/reactions | mutating + viewer+；反应身份=会话用户（body userId/userName 忽略） |
| PUT /[id]/tags | mutating + editor+ |
| GET/POST/PATCH/DELETE /api/folders | 登录；写法 mutating（工作区级资源，登录成员可管理——按要求保留宽松度） |
| GET/POST /api/tags、DELETE /api/tags/[id] | 同上 |
| GET /api/activity | 登录（工作区动态） |
| GET /api/export | 登录 + **仅 admin**（快照横跨全员私有文档），member 403 已实测 |

**server-activity.ts（归因防伪）**：`actorFromRequest` 改 async——先 `getSessionUser(req)`，有会话一律用会话用户（id/name/color，客户端无法再借 `x-z-actor` 冒名）；无会话回落 header（内部服务调用场景）。兼容处理：`logActivity`/`logActivityThrottled` 的 `actor` 参数类型放宽为 `ActivityActor | Promise<ActivityActor>` 且内部 `await`——我范围内 5 处调用点已全部 `await`，**范围外 sheets/slides/forms 的 8 处旧式调用（未 await）保持编译通过且同样自动获得会话归因**（tsc 必须无 src 错误，不能依赖并行代理的修改时机）。

**share-dialog.tsx**：invite 失败 toast 的 `e.message` 过 `t()`（dict-auth 已有 "Sign in required."/"You do not have permission to do that." 等中文词条，缺 key 自动回落英文原文）。

**curl 安全自测矩阵（dev :3000 实测，全部通过）**
1. 无 cookie：GET /api/documents → **401** `{"error":"Sign in required."}`；GET /api/documents/{id} → **401** ✓
2. admin 登录（admin@zdocs.test / Admin123456，-c /tmp/e1.jar）→ GET /api/documents → **200，15 篇全量可见**（14 篇 admin 私有 + 1 篇 legacy 夹具）✓
3. member（member@zdocs.test 已存在，Member123456 登录，-c /tmp/e1m.jar）→ GET /api/documents → **200 但 count=1，仅 ownerId=null 的 legacy 文档**，看不到任何 admin 私有文档 ✓
4. member 取 admin 私有文档（"Welcome to Z-Docs"）→ GET **404** `{"error":"Not found."}`；PATCH **404**（守卫规则「404 当 none」，比矩阵里的 403 更严格地隐藏存在性）；文档标题/editCount 复查未被改动 ✓；补充：member（viewer 级）PATCH legacy 文档 → **403** ✓；协作者流：admin 分享该文档给 member(editor) → member 列表 +1、GET 200、PATCH 200、尝试邀请协作者 403（非 owner）；admin 撤销 → member 立即 404 ✓
5. member POST /api/documents → **201**，响应 ownerId=member id（cmtnt4tuo…）✓
6. 限流：member 循环 300 次 GET → **第 238 次起 429**（240/min 桶，之前测试已占 2 次），共 63 个 429 ✓
7. 同源：无 Origin 的 POST（curl 默认）→ **201**（非浏览器客户端豁免）；伪造 `Origin: https://evil.com` 的 POST → **403 "Cross-origin request rejected."**（有无 cookie 均 403，origin 检查先于认证）；同源 Origin（http://localhost:3000）→ 201 ✓

**额外安全验证**：member export → 403 / admin export → 200；无 cookie activity → 401 / member activity → 200；伪造 `x-z-actor: {"id":"fake-admin"}` 发 POST → 活动流归属仍为 "Member Tester"（会话优先生效）；评论 body 伪造 authorName → 落库为会话用户；temp3（editor 非作者非 owner）PATCH 他人评论 → 403、reaction → 201（viewer+）；versions：shared 200 / 无权限 404；搜索组合（q+可见性 AND 合成）：member q="Untitled"（全为 admin 私有）→ 0 命中、q="Welcome"（被分享）→ 1 命中。

**测试数据清理**：5 篇测试文档（member 4 + legacy 夹具）经 admin DELETE 200 删除（连带评论/反应/协作者行级联）；9 条测试期活动行删除；临时账号 temp3@zdocs.test 删除（会话级联）。保留：admin@zdocs.test、member@zdocs.test（矩阵账号）；并行安全代理直插的 viewer 协作夹具行（cmtotestviewr…@Welcome doc）未动。未用 agent-browser、未重启 dev server、未改 prisma schema、未动 sheets/slides/forms/ai/collab-service。

**质量门**：`bunx tsc --noEmit | rg '^src/'` → **空**（仅 examples/skills 预存错误）；`bun run lint` → 本范围 17 文件 eslint exit 0（全仓 2 个 error 均在并行代理的临时文件 tmp-sec/collab-auth-test.js，非我范围）；dev :3000 全程 200。

Stage Summary:
- 核心文档 API 全部闭门：无 session 一律 401；列表按 owner/协作者/legacy 可见性过滤（admin 全量）；单文档 none→404 隐藏存在性；写操作按 editor/owner 分级；邀请协作者限 owner；导出限 admin；评论/反应身份服务端定死不可伪造；活动归因会话优先。
- 防攻击：所有 mutating 路由同源检查（跨源 403）+ 60/min 写限流，读 240/min；rateLimit/同源/会话均为 server-auth 既有原语，未引入新依赖。
- 兼容性：API 响应形状不变（仅新增守卫短路响应与 ownerId 字段语义）；前端 share-dialog 错误信息走 t()；sheets/slides/forms 的活动归因自动升级为会话用户而无需改其文件。
- 遗留提示（供主代理）：`documents/[id]/stats` 子路由在我接手前已被守卫（17-c 轮），文档 API 面现已全部有守卫；tmp-sec/ 临时文件归属并行代理待其自清。
