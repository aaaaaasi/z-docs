---
Task ID: 16-a
Agent: home/shell i18n subagent
Task: 首页/外壳/模板库/AppGrid 中文翻译

Work Log:
- Read worklog (last 2 sections), i18n.tsx, dict-common.ts, lang-toggle.tsx, doc-utils.ts, templates.ts + all owned components (home/*, app-grid, user-menu, doc-preview, avatar-stack, docs-store createDoc).
- src/lib/templates.ts: DocTemplate interface extended with nameZh/descriptionZh/titleZh/contentZh; all 7 templates given full Chinese metadata — contentZh is a complete HTML translation of content keeping tag structure, inline styles, &nbsp;, and the today() date interpolation (e.g. `titleZh: "会议记录 — " + today()`); blank contentZh = "". New export `localizedTemplate(tpl, lang)` returning per-lang {name, description, title, content}; `import type { Lang }` only (no runtime dep).
- src/lib/i18n/dict-home.ts: filled from empty TODO to 139 checked entries (shell, sidebar, template gallery, quickstart, docs grid, tag UI, app launcher, user menu, avatar tooltip). Keys are exact English sources — careful with apostrophe variants ("Couldn't create folder" straight vs "Couldn't create tag"/"Couldn’t create the …" curly), en-dash in "Title A–Z", curly quotes in "Delete “{title}” forever?" / "Moved to “{name}”" / " matching “{query}”". Interpolation keys: "{n} document(s)", "{n} words", "{n} 个文档"…, "Actions for {title}", "Open {title}", "Filter by tag {name}", "Use color {color}", " is editing" (leading space kept so en keeps "X is editing", zh renders "X正在编辑").
- home-view.tsx: footer "documents for teams" → 面向团队的文档协作 (Z-Docs wordmark untouched).
- home-header.tsx: DocsLogo aria "Z-Docs home", mobile nav aria/sr-only, both search placeholders + aria, settings aria/tooltip. <LangToggle/> left untouched as instructed.
- sidebar-nav.tsx: NewDocButton label/t/Toast, all nav items (All documents/Starred/Trash via t), Folders & Tags sections (headers, new buttons, tooltips, placeholders, toasts incl. unique-name errors, delete confirmations, empty states), Workspace section (Recent activity/Settings labels translated; Z-Sheets/Z-Slides/Z-Forms product names kept), Storage card. Renamed tags.map((t)→(tag)) to un-shadow i18n t.
- template-gallery.tsx: section/heading/aria labels, "Template gallery" link, AI card ("Help me write", "AI draft", "Draft with AI"), "Use template" hover chip; TEMPLATES.map((t)→(tpl)) un-shadow; each card renders localizedTemplate(tpl, lang).name/.description and DocPreview uses localized .content so zh previews show the Chinese body.
- doc-preview.tsx: emptyLabel default now t("Blank document") via useI18n (prop override still wins).
- quickstart.tsx: heading/aria, "More ways to start", per-app tiles via t(`New ${label.toLowerCase()}`) → 新建电子表格/演示文稿/表单, hints translated, failure toasts; POST body title uses tForLang(getCurrentLang(), opt.title) → 无标题电子表格/演示文稿/表单 in zh (en unchanged).
- docs-grid.tsx: header title via t(titleFor(...)) (Trash/Starred/Folder/Recent documents), doc count with "{n} document(s)" + " matching “{query}”", sort select (Last edited/Date created/Title A–Z), grid/list view aria, Retry, all toasts (Renamed/Deleted permanently/Moved to trash/Restored/Copy created/Moved to “{name}”/Removed from folder), rename dialog, delete-forever AlertDialog (title/description/buttons), MoveTo submenu (Move to/No folder), AddTags submenu (Add tags/Tag name/New tag name/New tag…/empty state/Tag created toast), CardMenu (Open/Remove star/Add star/Rename/Make a copy/Move to trash/Restore/Delete forever + "Actions for {title}" aria), DocCard/DocRow ("Open {title}" aria, Starred aria, In trash badge, Empty document, "{n} words", relativeTime(doc.updatedAt, lang) for zh relative dates), EmptyState fully translated (5 titles + 4 descriptions + Create a document). All .map((t)→(tg)) tag params renamed to avoid shadowing.
- doc-dnd.tsx: drag-end toasts localized via useI18n t().
- tag-ui.tsx: TagChip aria/title ("Filter by tag {name}"/"Filter by “{name}”"), TagOverflowChip title ("More tags: {names}"), TagFilterChip clear aria, TagColorPalette radiogroup aria + 6 color aria labels.
- app-grid.tsx: launcher aria "Z workspace apps", "Z Workspace" heading → Z 工作区, Recent activity/Settings rows. APP_META labels (Z-Docs/Z-Sheets/Z-Slides/Z-Forms) intentionally untranslated (product names).
- user-menu.tsx: Account settings aria, "Local account · demo", Display name/Avatar color labels, per-color aria, Save profile.
- avatar-stack.tsx: tooltip " is editing" → 正在编辑 (name kept bold via JSX span).
- src/store/docs-store.ts (createDoc only): template lookup now goes through localizedTemplate(template, getCurrentLang()); fallback "Untitled document" localized via tForLang (dict-common). No other store logic touched.
- Verification: node script cross-checks every t()/tForLang key used in owned files (incl. dynamic template-literal expansions) against dictHome+dictCommon → 139/139 present, 0 missing, 0 duplicate keys. bunx tsc --noEmit: 0 errors attributable to my files (full-src run currently shows only errors inside src/components/docs/editor/editor-header.tsx & editor/toolbar.tsx — the parallel editor i18n subagent's in-flight `}}` typos, outside my allowed file list; whole src minus those 2 files = 0 errors). bunx eslint on all my owned files: exit 0 clean (repo-wide `bun run lint` fails only on those same 2 editor files). dev.log shows continuous ✓ Compiled for my hot-reloaded changes; no compile errors.

Stage Summary:
- All home/shell/template-gallery UI strings now flow through the i18n system: default zh, switchable to en via the existing <LangToggle/>; product names, font names, DB content and shortcuts untouched.
- Templates are fully bilingual (metadata + HTML body), gallery previews render the Chinese body in zh, and createDoc/quickstart create localized document/spreadsheet/deck/form titles ("无标题文档" etc.) based on current language.
- dict-home.ts: 139 verified entries (no overlaps with dict-common; apostrophe/quote/dash variants preserved exactly so en-mode keys resolve).
- Quality gates for my scope: PASS (tsc 0 src errors from my files, eslint clean on my files, dev hot-reload compiles clean). Known repo-wide red flags at time of writing come exclusively from the concurrent editor-group subagent's half-finished edits (editor-header.tsx:123, toolbar.tsx:390 — extra `}`), which I'm not allowed to fix.
- relativeTime/fullTime: home grid now passes lang from useI18n; doc-utils zh formatting already built-in.
