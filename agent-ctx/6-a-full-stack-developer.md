# Task 6-a — full-stack-developer (doc tags)

## Task
Google Drive-style document tags (labels) for the Z-Docs home view: Prisma models, tags API, doc-tag wiring, sidebar Tags section, doc card chips, "Add tags" submenu, tag filtering.

## Work Log
- Prisma: added `Tag` (name @unique, color default #0b6b62) + `DocumentTag` (docId/tagId, @@unique, onDelete: Cascade both sides, indexes) + `Document.tags` relation; `bun run db:push` applied (Prisma client regenerated, dev server restarted with the documented `(setsid bun run dev >> dev.log 2>&1 &)` procedure — running next-server held the old client, per worklog Task 13 lesson).
- Types: `TagDTO { id, name, color, count? }`, `DocumentDTO.tags?: TagDTO[]`.
- API (all follow existing /api/folders conventions: db from `@/lib/db`, NextResponse, console.error + 500):
  - `src/app/api/tags/route.ts` — GET (tags + _count documents, name asc), POST { name, color? } (trim, slice 24, 400 empty, 409 duplicate, hex-validated color else random from the fixed 6-color palette).
  - `src/app/api/tags/[id]/route.ts` — DELETE (404 if missing; DocumentTag rows cascade).
  - `src/app/api/documents/route.ts` GET — `include: { tags: { select: { tag: { id, name, color } } } }`, toMeta maps + name-sorts tag DTOs.
  - `src/app/api/documents/[id]/route.ts` GET — same include (PATCH/DELETE responses unchanged, tags optional).
  - `src/app/api/documents/[id]/tags/route.ts` — PUT { tagIds } sets the EXACT tag set (deleteMany + createMany in $transaction, dedupe, cap 50, unknown ids dropped leniently, 400 on non-array, 404 doc). Does NOT touch doc.updatedAt (label changes are not content edits).
- Frontend — new `src/components/docs/home/tag-ui.tsx` shared primitives: TAG_COLORS fixed Google palette (#0b6b62/#d93025/#f9ab00/#1e8e3e/#a142f4/#5f6368), DEFAULT_TAG_COLOR, tagColorAlpha(hex,0.15) helper, `hitArea` (before:-inset-1.5 pseudo → ~32px hit targets for small controls), TagDot, TagChip (h-5 rounded-full bg-muted hover:bg-muted/70, dot + 11px name, click filters with stopPropagation), TagOverflowChip (+N with title tooltip), TagFilterChip (h-8, tag color @15% inline bg, ring-foreground/10, × to clear), TagColorPalette (role=radiogroup, 6 dots, selected ring).
- Store (docs-store.ts): `tags`, `tagFilter` state; `loadTags`, `setTagFilter`, `createTag` (POST + loadTags), `deleteTag` (optimistic remove from tags + all docs + clear filter, DELETE, refresh), `setDocTags` (no-op guard, optimistic chips, PUT, authoritative response tags, loadTags for counts, rollback on failure); hydrateFromUrl/goHome now also loadTags; toMeta passes tags through.
- Sidebar (sidebar-nav.tsx): "Tags" section below Folders — label + "＋" new-tag button (tooltip, hitArea); tag rows = colored dot + name + count, click toggles tagFilter (active = bg-accent), hover ⋮ menu → Delete tag (toast "Removed from all documents"); "All tags" clear row appears only while a filter is active; inline creation form = dot + Input (maxLength 24, Enter create / Escape cancel) + TagColorPalette (blur-cancel guarded with relatedTarget so palette clicks survive); empty-state copy. ALSO: wrapped nav/folders/tags/workspace in a `min-h-0 flex-1 overflow-y-auto` scroll container (storage card stays pinned at bottom) so many tags never clip the sidebar.
- Docs grid (docs-grid.tsx): `visible` memo filters `sorted` by tagFilter (composes with folder/starred/search/trash client-side); count line uses visible.length; Google Drive filter chip row under the header when activeTag (× clears); DocCard + DocRow render tag chips (max 3 + "+N", hidden in trash, click-to-filter, aria-pressed); CardMenu gains `AddTagsSubmenu` between "Make a copy" and "Move to": DropdownMenuCheckboxItem per tag (onSelect preventDefault keeps menu open for multi-toggle) + "New tag…" row → inline creation form inside the submenu (creates tag AND applies it to the doc); **Radix typeahead guard**: the Input's onKeyDown calls `e.stopPropagation()` so the menu's character typeahead can't steal keystrokes (verified Radix source fires typeahead for any bubbled printable key); EmptyState handles the tag-filter case.
- Verification (curl only, no agent-browser): db:push OK; GET /api/tags → []; POST (trim+24-cap confirmed), duplicate → 409, empty → 400; PUT on doc (dedupe, set semantics: shrink to one tag), GET doc + list show tags; counts correct; unknown tagIds dropped; bad body → 400; DELETE tag cascades (doc tags cleared, 0 orphan DocumentTag rows via direct Prisma count); DELETE repeat → 404. Test tags removed — DB left clean (0 tags, 10 docs untouched). `bun run lint` clean. SSR HTML contains the Tags section. dev.log clean.
- No edits to forbidden paths (src/components/docs/editor/*, globals.css, editor-dom.ts).

## Stage Summary
- Tags feature fully wired end-to-end: schema → API → store → sidebar / cards / menu / filter chip, styled to match the existing home aesthetic (13px rows, bg-accent actives, bg-muted chips, tnum counts, no heavy borders, dark-mode-safe via tokens).
- Key integration notes for future agents: the inline "New tag…" input inside the DropdownMenuSubContent REQUIRES `e.stopPropagation()` on its onKeyDown (Radix menu typeahead); sidebar nav area is now a scroll container; dev server was restarted after db:push (standard procedure).
- State: dev :3000 healthy, lint clean, user data intact (docs/folders/versions untouched), tags table empty for the user to populate.
