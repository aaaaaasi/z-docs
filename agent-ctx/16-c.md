---
Task ID: 16-c
Agent: sheets+slides i18n subagent
Task: Z-Sheets + Z-Slides 中文翻译

Work Log:
- Read context first: worklog.md (last sections + task 2-a/2-b scope), src/lib/i18n.tsx, dict-common.ts, lang-toggle.tsx, doc-utils.ts (relativeTime/fullTime take lang), docs-app.tsx (confirmed SheetsApp/SlidesApp render inside <I18nProvider>, so useI18n works app-wide incl. the present-mode portal).
- Read all 24 files under src/components/docs/sheets/** and src/components/docs/slides/** plus the two empty dict files; catalogued every user-visible string.
- Populated src/lib/i18n/dict-sheets.ts (84 entries) and src/lib/i18n/dict-slides.ts (115 entries). Keys are exact English sources (incl. curly-vs-straight apostrophe variants “Couldn’t…”/“Couldn't…”); shared sheets+slides strings (All / trash / star / copy titles / “edited {time}” / “Delete “{title}” forever?”) live only in dict-sheets since it merges before dict-slides; verified zero overlap with dict-common and zero duplicate keys across my two dicts.
- Z-Sheets translated: sheets-list.tsx (tabs All/Starred/Trash via t(tab.label), search placeholder/aria, empty states incl. “Nothing matches “{q}”.”, delete-forever AlertDialog + toast, count line via “1 spreadsheet”/“{n} spreadsheets”); sheet-card.tsx (aria labels, star tooltips, context menu, “{n} cells”, “edited {time}” with relativeTime(sheet.updatedAt, lang) — was missing the lang argument before); sheet-editor.tsx (title aria, SaveStatus Saving…/Unable to save/All changes saved to Z-Drive, formula bar “Enter a value or formula, e.g. =SUM(A1:A5)” + Active cell chip aria + fx kept, status bar Sum/Avg/Count → 求和/平均值/计数 via t("Sum: {n}", {n}) style + “Sheet1 · {rows} 行 × {cols} 列”, back/star/menu tooltips); sheet-toolbar.tsx (Undo/Redo/Bold/Italic/Align×3/Fill color/None/Clear formatting tooltips + aria); grid.tsx (Select column/row, Edit cell, Select all cells, Spreadsheet grid aria; col letter via colLetter()); sheet-list-actions.ts + sheet-store.ts + sheet-autosave.ts toasts + generated titles via tForLang(getCurrentLang(), …) (“Untitled spreadsheet”, “Copy of {title}”, Couldn't create/open/copy/save…, Moved to trash, Rename failed, localized listError “Something went wrong” / “Failed to load spreadsheets ({status})”).
- Z-Slides translated: slides-app.tsx (Loading Z-Slides aria, couldn't open/create toasts); slides-list.tsx (tabs, search, empty states, count + “ matching “{q}””, Retry); deck-card.tsx (menu, meta “{n} slides · edited {time}” with lang-aware relativeTime, In trash badge, rename/copy/trash/restore/delete-forever toasts + AlertDialog); deck-editor.tsx (save status incl. “Couldn’t save — retry”, speaker-notes panel “Slide {n} of {total}” + Notes placeholder, back/rename/star/layout/notes/present buttons + tooltips); present-mode.tsx (portal — useI18n works via React context: Presenting aria, End of presentation, Press Esc or click to exit, prev/next/exit tooltips with arrow hints, notes drawer “· slide {n} of {total}”, No notes for this slide.); slide-render.tsx (all six layout placeholders “Click to add title/subtitle/text/quote/section title/caption” — text only, interaction logic untouched; “Slide preview: {title}” aria; accent names stay English in the module-level ACCENTS data and are t()’d at render); layout-picker.tsx (t(l.label) for the six layout names Title/Title and body/Two columns/Quote/Section/Blank); theme-controls.tsx (Theme color popover + 6 accent names Teal/Forest/Amber/Rust/Plum/Slate, Sans/Serif 无衬线/衬线, hint line); slide-rail.tsx (per-slide menu Duplicate/Move up/Move down/Delete slide + undo toast, Go to slide {n}, New slide, PageUp/Down hint); slide-canvas.tsx (zoom tooltips Ctrl±, prev/next slide, empty state, Add a slide, Slide title/text aria); deck-store.ts (tForLang for “Untitled presentation”, “Copy of {title}”, localized listError).
- Added <LangToggle /> (ghost icon h-9 w-9 rounded-full, matching existing header buttons) to BOTH editor headers: deck-editor.tsx (plain, fits 375px since star/save/theme/layout are responsive-hidden on mobile) and sheet-editor.tsx (wrapped in <span className="hidden min-[420px]:inline-flex"> so the long-title + saving-status mobile header cannot overflow; ≥420px viewports show it).
- Non-translated by design: Z-Sheets/Z-Slides product names, A–Z column letters & row numbers, formulas (SUM…), error values (#ERROR!/#DIV/0!/#CIRC!/#NAME?), fx mark, hex color codes, DB content (deck/slide user data), numeric counters, Sheet1 tab label.
- cells.ts / formula-parser.ts / formula.ts / sheets-app.tsx contain no user-visible English UI copy (only comments + error codes) — verified, unchanged.
- Verification script: extracted every t()/tForLang() key from my 24 files and diffed against dictCommon+dictSheets+dictSlides → 0 missing, 0 unused (except the 13 dynamic-label entries rendered via t(l.label)/t(a.name), as designed). Caught & fixed 2 real gaps this way (missing “Delete slide”, removed unused “Untitled”).
- Quality gates: `bunx tsc --noEmit 2>&1 | rg '^src/'` → empty; `bun run lint` → exit 0. (Transient cross-agent note: during my run, editor-header.tsx:123 / toolbar.tsx:390 had an in-flight extra-`}` typo from the editor i18n agent that briefly failed the global gate — it was fixed by that agent before my final run; my scope was clean throughout.)
- No agent-browser use, no dev-server restart, no build, no edits outside my owned files.

Stage Summary:
- All 24 sheets/slides component files fully wired through the i18n system (useI18n in components, tForLang(getCurrentLang(), …) in zustand store actions/autosave, relativeTime/fullTime with lang) — default Chinese, switchable to English via the two new editor-header <LangToggle />s.
- Deliverables: dict-sheets.ts = 84 zh entries, dict-slides.ts = 115 zh entries (199 total; zero collisions with dict-common, zero cross-dict duplicates); all t() keys verified 1:1 against component sources by script.
- Status-bar stats, formula bar, toolbar tooltips, list pages, empty states, all toasts, present mode (end card / notes drawer / controls), theme controls and layout names now render Chinese by default with graceful English fallback.
- Generated copy titles (“Copy of {title}” → “{title} 的副本”) and default titles (“Untitled spreadsheet/presentation” → 无标题电子表格/无标题演示文稿) are localized at creation time; stored user data is never rewritten.
- Quality gates pass: tsc clean, eslint clean, dev server hot-reloaded without errors (dev.log ✓ Compiled).
