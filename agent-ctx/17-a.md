---
Task ID: 17-a
Agent: multi-select subagent
Task: 文档多选 + 批量操作栏
Work Log:
- 读 worklog 末两节 + i18n.tsx/lang-toggle.tsx 契约（t(key, params)、zh 词典按组独占、key=英文原文）+ docs-store/docs-grid/home-view/doc-dnd 现状。
- docs-store.ts（独占）：新增 `selection: string[]` + `toggleSelect` / `selectAll`（去重）/ `clearSelection`；新增 `BatchOp` 类型（star|unstar|trash|restore|deleteForever|move）与 `batchOp(ids, op, folderId?)` —— 循环调用 `api()`（@/lib/api-client，带 x-z-actor 活动归因）：star/unstar→PATCH {starred}、trash/restore→PATCH {trashed}、move→PATCH {folderId}（null=主页根）、deleteForever→DELETE；逐项统计 ok/failed（403/404/网络异常计 failed，不抛出）；有成功项后 refresh(silent) + move 时 refreshFolders。setFilter / openFolder 切视图时清空选择（Google Drive 行为）。
- docs-grid.tsx（独占）：
  - `SelectCheckbox`：圆形 Google 风格复选框（选中=primary 填充白勾，未选=白底描边），按钮本身 48px 命中区（h-12 w-12，含 padding），`aria-label={t("Select {title}")}`、role=checkbox/aria-checked；onClick/onPointerDown/onKeyDown 全 stopPropagation（防卡片开文档、防 dnd-kit 拖拽误触、防键盘双触发）。
  - 网格卡片：预览容器左上角复选框（hover/已有选中/focus-within 时显示，否则 pointer-events-none）；选中卡片 `bg-primary/5 ring-1 ring-primary/40`；回收站"In trash"角标在复选框可见时淡出避免重叠。
  - 列表行：左端 48px 槽位，FileText 图标 ↔ 复选框淡入淡出切换；选中行 primary 淡底 + inset ring。
  - selection 非空时点击卡片/行主体=toggleSelect（不开文档）；MoreVertical 菜单按钮仍打开单文档菜单（原有 stopPropagation 保留）；Enter/Space 键盘行为同步分支。
  - 顶部选择状态条：`{n} selected`（已选 {n} 项）+ 全选（当前过滤视图 visible 项，全选后禁用）+ 取消选择。
  - `BatchActionBar`：`fixed bottom-4 left-1/2 -translate-x-1/2 z-50` 圆角胶囊（bg-popover/95 + backdrop-blur + 自定义 elev-4 级双层阴影 + dark 变体，深浅色自适应），内容：已选计数 | 星标 | 取消星标 | 移动到（DropdownMenu：主页根 + folders 彩色图标）| 移至回收站（非 trash 视图）/ 恢复 + 永久删除（trash 视图，destructive 色）| 关闭(X)。全部图标按钮带 Tooltip + aria-label；执行中该按钮显示 Loader2 spinner、全部按钮 disabled；移动端 `max-w-[calc(100vw-2rem)] overflow-x-auto no-scrollbar`（375px 实测内容 ~270px 直接放得下）。
  - **createPortal(document.body)** 渲染批量栏：docs-app 的 `animate-view-in`（fill-mode both → 持续 transform）会成为 fixed 后代的 containing block（同 worklog Task 2-b present-mode 结论），portal 才能真正固定视口底部。
  - Escape 清空选择：DocsGrid 挂 window keydown，带守卫（事件源自 menu/dialog/alertdialog/listbox/input/textarea/contenteditable 时不清，避免关菜单连带清空选择）。
  - 批量永久删除走 AlertDialog 确认（与单删除一致）；完成后 toast：成功 `{n} 篇文档已…`（按 op 六种文案，move 带“{name}”）、失败另发 destructive toast `{n} items failed`；随后 clearSelection。
- home-view.tsx：零改动（docs-grid 直接消费 store，批量栏内置）。
- dict-multi.ts（独占）：填入 23 条 zh 词条（选择/批量栏/六种批量结果/失败/批量删除确认），与既有词典重复的 key（Select all/Move to/Restore 等）按原文一致复写，合并无冲突。
- 验证：`bunx tsc --noEmit` 无 src 输出；`bun run lint` 干净；自写 key 覆盖脚本（docs-grid 99 个 t() key 与 12 词典合并交叉比对）缺失 0；API 契约真实打点（临时注册账号 → PATCH starred/trashed/folderId:null、DELETE、404 假 id 计失败 → 全部符合预期 → 测试数据已清理）；dev :3000 编译 200、dev.log 无错误（未重启服务、未用 agent-browser、未 build）。
Stage Summary:
- 首页文档多选全链路可用：hover 出现圆形复选框（48px 命中区）或点击复选框进入选择模式 → 卡片/行点击切换选中 → 顶部"已选 n 项 + 全选 + 取消选择" → 底部浮动批量操作栏（星标/取消星标/移动到文件夹含主页根/移至回收站或恢复+永久删除/关闭）→ 逐项调 PATCH/DELETE API → spinner 禁用态 → 成功/失败 toast → 刷新列表 + 清空选择；Escape 清空选择；切过滤器自动清空；移动端 375px 不溢出；全 UI 文案 t() 化，zh 词条在 dict-multi.ts（23 条）。
- 改动文件：src/store/docs-store.ts（+selection/batchOp，仅追加与两处 setFilter/openFolder 清选择）、src/components/docs/home/docs-grid.tsx（多选 UI + 批量栏）、src/lib/i18n/dict-multi.ts（23 词条）、src/components/docs/home/home-view.tsx（未动）。
- 质量门：tsc 0 src 错误、lint 干净、dev :3000 200。
