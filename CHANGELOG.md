# Changelog

本项目的所有重要变更都记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本管理遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-08

首个开源版本。

### 新增

#### 文档编辑器（Z-Docs）
- 自研富文本编辑内核：加粗/斜体/下划线/删除线、文字颜色、高亮、字体字号、对齐、行距、列表、缩进、链接、图片
- 信纸白页纸张视图、可拖动边距标尺、缩放、页面/字数/字符实时统计
- 评论系统：选区评论、回复串、表情反应、解决状态
- 版本历史：自动快照、预览、版本间 diff、一键恢复
- 文档大纲侧栏（标题跳转）、查找替换、Emoji 选择器、字数统计面板
- 拼写检查（AI 检测 + 修正建议一键应用）、AI 帮我写 / AI 润色、语音输入
- 写作洞察：历程 8 项指标 + 鉴赏 14 项指标（中英双语）
- 表格：尺寸选择插入、单元格编辑、上下文菜单
- 多格式导出：PDF（服务端 Chromium 矢量渲染）、DOCX（真 OOXML）、HTML、TXT、打印视图
- 模板库：空白 / 会议记录 / 项目提案 / 信函 / 简历（真实内容）
- 分享协作者、在线协作状态头像

#### 电子表格（Z-Sheets）
- 网格编辑、公式引擎（SUM、区间引用、循环引用检测 `#CIRC!`）
- 单元格格式（加粗/斜体/对齐）、列选择、自动保存

#### 幻灯片（Z-Slides）
- 多布局版式、主题色、衬线/无衬线字体
- 缩略图轨道、全屏演示模式（键盘翻页）、演讲者备注

#### 表单（Z-Forms）
- 6 种题型（简答/段落/单选/多选/下拉/星级）、必填开关、选项管理
- 受访者预览、提交、回复统计汇总

#### 平台
- 账号体系：注册 / 登录 / 会话（Sec-Fetch-Site 同源校验）
- 游客模式：数据仅存本设备（localStorage），登录即升级云端
- 实时协作：Yjs + Socket.io 独立服务（在线状态、远程光标、文档同步）
- 存储真实用量统计（15GB 配额）、近期动态活动流（按应用分组）
- 文件夹、标签、星标、回收站、批量操作（多选）
- 中英双语完整 i18n、深色模式、移动端响应式（375px 适配）
- 无障碍：语义化 landmark、aria 标签、键盘导航
- API：40+ REST 路由（documents / sheets / slides / forms / comments / folders / tags / auth / activity / storage / export / ai）

### 工程质量
- TypeScript strict 全覆盖、ESLint 零告警
- 10 轮 agent-browser E2E 审查（含移动端视口、深色/双语、游客/登录态）全程零控制台错误
- PDF/DOCX/HTML/TXT/CSV 全格式导出保真度审计（pdftotext 文字层 + VLM 视觉审查）
- 注册/登录/403 拒绝路径/游客模式全链路实测

## [1.1.0] - 2026-09-08

### 新增

#### 写作洞察（Ellipsus 对标）
- **实时洞察侧栏**（编辑器右栏，Ellipsus 官方规格）：写作时 ~0.45s 防抖实时重算全部指标；长文档显示「分析中…」状态；节奏柱/段落密度点/句子片段点击即在文档中定位；头部与「查看」菜单双重开关
- 写作工作室深潜对话框保留：历程 8 项 + 洞察 15 项指标的完整网格视图

#### 图片嵌入
- **远程图片冻结为 data-URL**（插入时）：通过自建 `/api/image-proxy` 中继（SSRF 防护：私有网段/端口/协议拦截，5MB 上限，image/* 白名单），文档自此自包含——导出不脏画布、外链失效不丢图
- PDF 导出预转：html2canvas 兜底路径先冻结后截取、finally 恢复原 src（绝不静默改写已保存文档）

#### 导出体验
- **导出进度中心**（Google Drive 式右下角下载卡片）：确定性百分比进度条 / 未知长度流光条、字节级下载进度（fetch body reader）、分页渲染「第 i 页，共 n 页」、完成卡 4.2s 自动收起、aria-live 无障碍播报
- 覆盖 PDF / DOCX / HTML / TXT / PPTX / CSV / JSON 备份全部导出管线

#### 批量操作
- 批量打标签（union 语义，绝不误删已有标签）
- 批量重命名：查找替换 / 编号模板（`{n}`/`{title}` 占位符）+ 实时预览，仅 PATCH 实际变化的文档

#### 幻灯片
- Z-Slides PPTX 导出（pptxgenjs 客户端动态加载，标准 OOXML，python-pptx 校验通过）

#### 设置
- 游客本地数据管理界面：本地数据库统计、JSON 备份导出（含进度卡片）、清空本地数据、资料重置

### 修复
- `ExportProgressPanel` 挂载于根 layout 时 `useI18n` 脊柱错位崩溃（整站 500）——`I18nProvider` 提升至根 `Providers` 全局挂载
- 游客模式 fetch shim 拦截 `/api/image-proxy` 返回 404 —— 加入透传名单

## [1.1.1] - 2026-09-09

### 新增

#### 写作洞察中文原生（联网研究驱动）
- **中文可读性公式**：0–100 难度分 + 五档等级（入门/偏易/适中/偏难/高难），由平均句长、生僻字率、超长句占比加权——锚定中文可读性研究（句长 >30 字难度明显上升、>40 字几乎必然偏难），中文文档不再显示「弗莱士-金凯德 / 适合 16 年级」这类英语公式
- **中文被动语态检测**：被字句标志词（被 / 受到 / 遭到 / 遭受 / 经受 / 蒙受 / 深受 / 备受 / 为…所 / 由…所），寝具名词护栏（被子/被单/被套不误报）；使役弱标记（叫/让/给）刻意不计——与使役句无法可靠区分，宁缺毋滥
- **中文副词检测**：60+ 高置信度副词词表（非常/十分/特别/突然/立刻/渐渐…）+ 「地」字短语；名词子串护栏（最后/太太/迁就/归还/再见/成就/都市/非常规/原来的 不误报）
- **中文词频与词汇多样性**：无分词场景以相邻二字组合（bigram）近似词——「市场 ×3」比单字统计更贴近词频直觉
- **句长分档按语言**：中文以字（≤15 / 16–30 / 31–40 / >40），英文以词（≤5 / 6–14 / 15–25 / >25）；中文文档隐藏「冠词」开头行（英语概念）

#### 字数统计（Word 口径）
- `countWords` 全局重写：每个 CJK 字符计 1 + 每个连续拉丁/数字词组计 1（微软 Word「字数」规则）——纯中文文档不再「整段算一个词」（1,987 字文档曾显示「42 词」）
- 字数统计对话框新增 Word 式行：页数 / 字数 / 字符数（不计空格）/ **非中文单词** / **中文字符** / 段落数 / 阅读时间
- 阅读时间按语言加权：中文 300 字/分钟（成人平均默读）+ 英文 200 wpm；页数估算中文 900 字/页
- 修复覆盖 9 处调用点：编辑器底栏、文档列表 wordCount、写作历程「总字数」、AI 工具字数、服务端 API（游客/云端）

#### 编辑器
- 文档标题输入框精确测宽：隐形 sizer span 按真实字形（含 CJK 双宽）测量——长中文标题完整显示不再截断；悬停显示完整文件名；空态最小 8ch 舒适输入
- 首页文档卡片/列表行标题补充 `title` 悬停提示

### 修复
- 段落密度中文权重重校准（0.33 缩放，25 字/句的中文段落不再误判「复杂」）

### 安全
- `npm audit` 专项：**32 → 6 漏洞，critical 2 → 0**
  - 升级 next 16.1.3 → 16.3.4（critical：postcss/sharp 链）、next-auth → 4.24.15（critical）
  - 移除未使用依赖 @mdxeditor/editor（js-yaml 高危链）、react-syntax-highlighter（prismjs 链）
  - sharp → 0.35.4；overrides 强制 14 个易受攻击传递依赖到修复版（brace-expansion/browserslist/minimatch/picomatch/lodash/defu/flatted/js-cookie/ajv 等）
  - 剩余 6 项高危均为无前向修复的开发链（prisma CLI 配置加载、pptxgenjs 内嵌 image-size、eslint 插件嵌套 minimatch ReDoS）——全部仅开发期运行，服务端运行时不受影响，已评估接受

## [1.1.2] - 2026-09-09

### 修复（重大）

#### 字号调节被 Chrome 新行为冻结在 36pt（用户实测报告）
- **根因（联网研究确认）**：Chrome ≥119 的 `execCommand("fontSize")` 不再输出 `<font size="7">`，而是直接生成 `<span style="font-size: xxx-large">`（48px = 36pt）。旧代码的 `font[size="7"]` 替换循环因此永远匹配不到——选中文本无论按 + 还是 −，文字全部停在 36pt 且无法恢复
- `applyFontSize` 重写：命令后同时归一化传统 `<font size="7">` 与新版关键词 span（`span[style*="xxx-large"]`）到精确 pt 值；历史遗留的 xxx-large 文本也会被下一次调节顺带修复
- 步进器语义重做：+ = 严格大于当前值的最小标准字号、− = 严格小于的最大标准字号（当前值不在列表时按邻近档处理，不再跳回最小值）；到达上下限时按钮置灰

#### 存储数据造假（15GB 虚构配额）
- 移除硬编码 15GB 配额：`/api/storage` 的 quota 改为 `STORAGE_QUOTA_GB` 环境变量可选配置，默认 0（未知）
- 侧栏存储卡与设置页在无配额时只显示**真实测算用量**与条目数（「已使用 13.1 KB · 2 项内容」），不再渲染虚构总量与进度条；游客本地模式同步去除
- 顺带清理未使用的「1.8 GB of 15 GB used」占位词条

#### 模板库假链接
- 「模板库」曾是 `cursor-default` 的装饰性 span（看起来可点击但无任何行为）——改为真实按钮，打开**完整模板库对话框**（2/3/4 列响应式网格、更大预览、含空白/AI/全部模板，一键创建并关闭）；移动端同样可用

### 新增

#### 全文搜索（多关键词 + 匹配高亮 + 点击定位）
- **多关键词 AND 匹配**：空格分词，每个词都必须命中标题或正文（服务端 Prisma 与游客本地模式同语义）
- **匹配上下文片段**：每篇结果文档返回命中总数 + 最多 3 个段落级上下文片段（首命中 ±窗口、省略号截断）；标题命中回退为标题片段
- **结果卡片高亮**：网格卡显示命中徽标（「N 处匹配」）+ 片段内所有命中词 `<mark>` 高亮（琥珀色，深色模式适配）；列表视图显示全部片段
- **点击直达定位**：从搜索结果打开文档时自动弹出查找面板并带入搜索词——文档内全部命中即时高亮 + 1/N 计数器导航；多词短语无字面命中时回退首个词

#### 中文字体（联网研究驱动）
- 全局 UI 字体栈：Geist（拉丁）+ 苹方 PingFang SC → Hiragino Sans GB → 微软雅黑 → 思源黑体 Noto Sans SC 回退（跨平台系统栈，零下载）
- 文档页面默认字体、编辑衬线、等宽栈同步补 CJK 回退
- **字体选择器双语扩展**：新增 6 个中文条目（苹方 / 微软雅黑 / 思源黑体 / 宋体 / 楷体 / 黑体），每条为跨平台字体栈；条目内「永字」实时预览样张

#### 三端响应式（手机 <768 / 平板 768–1024 / 桌面 >1024，Material Design 3 规范）
- **Z-Slides**：手机端缩略图栏从「直接隐藏」改为**底部胶片条**（横向滚动缩略图 + 数字角标 + 常显 ⋮ 菜单 + 末尾新建卡）——新建幻灯片/复制/上移/下移/删除在手机全部可达；主题色与版式收进「更多操作」面板；平板收窄 192px；桌面不变
- **编辑器三侧栏**：手机全宽浮层 + **遮罩**（点击文档即关闭，模态抽屉式）；平板 288–360px 中间档浮层；桌面 dock 不变。**右栏互斥**（评论/洞察同列切换，避免 1024px 双开挤死画布）；查找面板在窄屏浮层打开时不再消失（z-40 置顶）
- **Z-Forms**：<1024 保存状态改为状态圆点；手机星标入溢出菜单
- **Z-Sheets**：语言切换在 <640 收进更多菜单；自定义 420px 断点标准化
- 编辑器 900px 自定义断点统一对齐 1024（平板走窄页模式）；菜单栏移动触点 44px；加载骨架移动端收窄
- 文档画布窄页 CSS/JS 双处统一 1024 语义

#### 起始面板视图感知
- 「开始新文档 / 模板库 / 更多开始方式」面板**只在「全部文档」视图显示**——星标/回收站/文件夹/标签筛选与搜索结果视图不再常驻该面板（Google Drive 行为对齐）

### 工程
- README 中英双语新增搜索截图与功能条目；重截 home/editor 截图（诚实存储卡 + 新字体工具栏）
- i18n 新增词条（模板库对话框 / 匹配数 / 存储诚实文案）

## [Unreleased]

### 计划中
- 拼写检查与 Ctrl+Z 撤销栈整合
- Ellipsus 草稿（draft）/合并（merge）工作流
- 更多语言的可读性估算（德/法/西/葡）
